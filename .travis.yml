sudo: false
language: java
jdk: oraclejdk8
branches:
  only:
    - develop
before_install:
  - mvn versions:set -DnewVersion=$TRAVIS_BUILD_NUMBER
after_success:
  - bash <(curl -s https://codecov.io/bash)